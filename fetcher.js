/*import fetch from 'node-fetch';

async function asyncRequest(url, method, accessToken) {
    var data = await fetch(url, {
        method: method,
        headers: {
            "Authorization": "Bearer " + accessToken,
            "Content-type": 'application/x-www-form-urlencoded'
        }
    });

    return await data.json();
}

export function request(url, method, accessToken) {
    return asyncRequest(url, method, accessToken);
}
*/